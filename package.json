{
  "name": "gustav",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "bun build src/renderer.ts --outfile=dist/renderer.js --target=browser --minify",
    "start": "bun run build && electron .",
    "postinstall": "electron-rebuild"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "electron": "^35.0.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.0"
  }
}
